https://www.youtube.com/watch?v=MykcjWPJ6T4

Embedding Model - sentence transformer from huggingface
langchain
langchain-core
langchain-community
pypdf
pymupdf
sentence-transformers
faiss-cpu
chromadb
langchain-groq
python-dotenv


# step 1 requirements.txt
# step2  Dockerfile
FROM python:3.11-slim
WORKDIR /app
# Install uv for faster package managemet
RUN pip install uv

# COPY requirements.txt
COPY requirements.txt .

# Install dependencies using uv
RUN uv venv

RUN uv add -r requirements.txt

# COPY application code
COPY server.py .
COPY client-sse.py .

# Expose the Port the server run on
EXPOSE 8050

# Command to Run the server
CMD ["uv","run","server.py"]

docker build -t mcp-math-server .
docker run -it mcp-math-server
