Retrieval-Augmented Generation (RAG) is the process of optimizing the output of a large language model, so it references an authoritative knowledge base outside of its training data sources before generating a response. Large Language Models (LLMs) are trained on vast volumes of data and use billions of parameters to generate original output for tasks like answering questions, translating languages, and completing sentences. RAG extends the already powerful capabilities of LLMs to specific domains or an organization's internal knowledge base, all without the need to retrain the model. It is a cost-effective approach to improving LLM output so it remains relevant, accurate, and useful in various contexts.

Agenda:
 Document Structure:
  
	
	



RAG- Retrieval Augmented Generation (RAG): provide context for AI Model
LLM can't answer correctly to private data.
LLM can't provide the real time information

RAG allows LLM to use external resources for better response
	more accurate answers to private data
	Stay current with new information
	
RAG:
	Retrieval
		embadding- convert the data into numeric representation,store that numeric representation in vector database (Pinecone).
	Augment
		set context with query(prompt)
	Generation
		LLM: based on context+ prompt generate response	
		
Embedding:		
	Load:
		Data
	Split:
		break it to small (splitted), chunk
	Embed:
		chunk data into number
	Store:
		store numbers from embedding in vector Database (Pinecone)
		search using cosine similarty
		