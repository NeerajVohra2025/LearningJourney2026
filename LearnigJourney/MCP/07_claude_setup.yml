step1: Install claude desktop
step2: create a folder and create 2 text files inside the folder
step3: mcp server paths  
    https://github.com/modelcontextprotocol 
step4: cloud desktop-> setting-> developer->edit config ==> claude-desktop-config  #"C:\\Learning2025"
step5 : cloud desktop-> file->exit

"airbnb": {
      "command": "npx",  # node js and npx must be installed
      "args": [
        "-y",
        "@openbnb/mcp-server-airbnb",
        "--ignore-robots-txt"
      ]
    }
    
plugin:
 cline 
    